{
  "name": "rope-untangling-lab",
  "version": "0.5.4",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --test",
    "check": "node --check src/constants.js && node --check src/main.js && node --check src/topology.js && node --check src/geometry.js && node --check src/solver.js && node --check src/render-order.js && node --check src/game-start.js"
  },
  "devDependencies": {
    "vite": "^7.1.7"
  }
}
